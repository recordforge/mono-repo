{
  "name": "Mono Repo Dev Container",
  "build": {
    "dockerfile": "Dockerfile.devcontainer",
    "context": "."
  },
  // ...existing code...
  "runArgs": [
    "--privileged", // Needed for Docker-in-Docker and Minikube
    "-v", "/var/run/docker.sock:/var/run/docker.sock"
  ],
  "mounts": [
    "source=devcontainer-docker,target=/var/lib/docker,type=volume",
    "source=${localEnv:HOME}${localEnv:USERPROFILE}/.ssh,target=/home/vscode/.ssh,type=bind,consistency=cached"
  ],
  "remoteUser": "vscode",
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "bash -c 'echo \"# GitHub CLI and Git setup\" >> ~/.bashrc && echo \"gh auth setup-git 2>/dev/null || true\" >> ~/.bashrc && echo \"git config --global user.name \\\"\\$(gh api user --jq .login 2>/dev/null || echo vscode)\\\"\" >> ~/.bashrc && echo \"git config --global user.email \\\"\\$(gh api user --jq .login 2>/dev/null || echo vscode)@users.noreply.github.com\\\"\" >> ~/.bashrc && source ~/.bashrc && echo \"Dev container ready.\"'",
  "forwardPorts": [3000, 8000, 8080],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",                // Python support
        "ms-python.vscode-pylance",        // Python language server
        "ms-toolsai.jupyter",              // Jupyter notebooks
        "ms-vscode.makefile-tools",        // Makefile support
        "ms-azuretools.vscode-docker",     // Docker support
        "ms-kubernetes-tools.vscode-kubernetes-tools", // Kubernetes support
        "github.vscode-pull-request-github", // GitHub integration
        "cschleiden.vscode-github-actions" // GitHub Actions support
      ],
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
      }
    }
  }
}
